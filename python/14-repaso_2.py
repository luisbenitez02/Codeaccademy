def distancia_desde_cero(num):

    if type(num)=="int" or type(num)=="float":
       print(abs(num))
    
    else:
        return "¡Esto no es un entero ni un real!"
#ESTE CODIGO NO FUNCIONA, type no parece devolver datos en string
